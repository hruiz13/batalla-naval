import React from 'react';
import ReactDOM from 'react-dom';
import './styles/styles.scss';
import BatallaNavalApp from './BatallaNavalApp';

ReactDOM.render(
  <React.StrictMode>
    <BatallaNavalApp />
  </React.StrictMode>,
  document.getElementById('root')
);

